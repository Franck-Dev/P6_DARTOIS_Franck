{% extends 'base.html.twig' %}

{% block title %}New Trick{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-xs-12 col-sm-8 col-md-6 col-lg-5 bg-light mt-5 p-5 border border-dark">
        {% for error in errors %}
            <div class="alert alert-danger">{{ error.message| trans | capitalize }}</div>
        {% endfor %}
        <h1>Creation Trick</h1>
        <div class="text-center p-1">
            {{ include('tricks/_form.html.twig') }}
        </div>
    </div>
</div>
    {# <a href="{{ path('tricks_index') }}">back to list</a> #}
{% endblock %}
